# 用map来处理字符串列表，把列表中所有人变成alex_sb
# name = ['oldboy','alex','wusir']
# def square(x):
#     return x+'_sb'

# name = map(square,name)
# print(list(name))

# name = map(lambda x:x+'_sb',name)
# print(list(name))